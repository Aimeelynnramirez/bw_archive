#include <cs50.h>
#include <stdio.h>

//   \n keep this for the back slash do not know from this key base lol... 
// this arg remember this fo array strings before input (on the ./"name" char or number value)
int main(int argc, string argv[])
{
    if (argc == 2) 
{  // because this is a number and we need to pass the character text in 
    // one word will get a number one value on string so it will print out above 

    printf("hello this is a file is called, %s\n", argv[0]);

} 
else
{    // look this is to be a hash
    // lala gets a number one value on string so it will print out above 
     printf("hello that is what I needed a text \n"); 

  }
}

//syntax!!! 